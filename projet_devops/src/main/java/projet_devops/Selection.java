package projet_devops;

public class Selection {
    

    public DataFrame selectIndex(DataFrame df, int start, int end) {
        DataFrame newDf = new DataFrame();
        for (int i = start; i < end; i++) {
            
        
        }

        return newDf;
    }  
}
